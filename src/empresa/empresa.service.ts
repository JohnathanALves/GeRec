import { Injectable } from '@nestjs/common';
import { Repository } from 'typeorm';
import { Empresa } from './empresa.entity';

@Injectable()
export class EmpresaService {
  // constructor(private readonly empresaRepository: Repository<Empresa>) {}
}
